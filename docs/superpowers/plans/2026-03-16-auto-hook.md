# Auto Hook Implementation Plan

> **For agentic workers:** REQUIRED: Use superpowers:subagent-driven-development (if subagents available) or superpowers:executing-plans to implement this plan. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add a Stop hook that auto-triggers commit/push when CC finishes a task, configurable via `/smart:config`.

**Architecture:** Command-based Stop hook reads config from `.claude/smart.local.md`, checks git status, and blocks Stop with a systemMessage instructing Claude to invoke the corresponding skill. Loop prevention via git status: clean tree = approve.

**Tech Stack:** Bash (hook script), Markdown with YAML frontmatter (skill + config)

**Spec:** `docs/superpowers/specs/2026-03-16-auto-hook-design.md`

---

## Chunk 1: Hook Infrastructure

### Task 1: Create hooks.json

**Files:**
- Create: `plugins/smart/hooks/hooks.json`

- [ ] **Step 1: Create hooks.json**

```json
{
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "bash ${CLAUDE_PLUGIN_ROOT}/hooks/auto-commit.sh",
            "timeout": 15
          }
        ]
      }
    ]
  }
}
```

- [ ] **Step 2: Commit**

```bash
git add plugins/smart/hooks/hooks.json
git commit -m "feat: add Stop hook configuration for auto commit/push"
```

### Task 2: Create auto-commit.sh

**Files:**
- Create: `plugins/smart/hooks/auto-commit.sh`

- [ ] **Step 1: Create auto-commit.sh**

```bash
#!/bin/bash

# Drain stdin to prevent broken pipe
cat > /dev/null

# Fallback: any error → approve (never block unexpectedly)
approve() { echo '{"decision": "approve"}'; exit 0; }
trap approve ERR

# 1. Guard: CLAUDE_PROJECT_DIR must be set
if [ -z "${CLAUDE_PROJECT_DIR:-}" ]; then
  approve
fi

# 2. Guard: git must be available
if ! command -v git &> /dev/null; then
  approve
fi

# 3. Read config
CONFIG_FILE="$CLAUDE_PROJECT_DIR/.claude/smart.local.md"
AUTO_ACTION="off"

if [ -f "$CONFIG_FILE" ]; then
  AUTO_ACTION=$(awk '/^---$/{n++; next} n==1 && /^auto_action:/{gsub(/^auto_action:[[:space:]]*/, ""); gsub(/["'"'"']/, ""); gsub(/[[:space:]]/, ""); print; exit}' "$CONFIG_FILE")
fi

# 4. Config off or invalid → approve
if [ -z "$AUTO_ACTION" ] || [ "$AUTO_ACTION" = "off" ]; then
  approve
fi

# 5. Check for uncommitted changes
if git -C "$CLAUDE_PROJECT_DIR" diff --quiet 2>/dev/null && \
   git -C "$CLAUDE_PROJECT_DIR" diff --cached --quiet 2>/dev/null && \
   [ -z "$(git -C "$CLAUDE_PROJECT_DIR" ls-files --others --exclude-standard 2>/dev/null)" ]; then
  approve
fi

# 6. Dirty tree → block and instruct Claude
if [ "$AUTO_ACTION" = "push" ]; then
  SKILL="/smart:push"
else
  SKILL="/smart:commit"
fi

cat <<EOF
{"decision": "block", "reason": "auto-action triggered", "systemMessage": "Auto ${AUTO_ACTION} is enabled. Uncommitted changes detected. Invoke ${SKILL} now without asking for confirmation."}
EOF
exit 0
```

- [ ] **Step 2: Make executable**

```bash
chmod +x plugins/smart/hooks/auto-commit.sh
```

- [ ] **Step 3: Test script locally — config off (default)**

Use a temp directory as `CLAUDE_PROJECT_DIR` to avoid polluting the repo:

```bash
TEST_DIR=$(mktemp -d) && git init "$TEST_DIR" > /dev/null 2>&1 && export CLAUDE_PROJECT_DIR="$TEST_DIR" && echo '{}' | bash plugins/smart/hooks/auto-commit.sh
```

Expected: `{"decision": "approve"}`

- [ ] **Step 4: Test script locally — config commit, clean tree**

```bash
mkdir -p "$TEST_DIR/.claude" && cat > "$TEST_DIR/.claude/smart.local.md" << 'TESTEOF'
---
auto_action: commit
---
TESTEOF
echo '{}' | bash plugins/smart/hooks/auto-commit.sh
```

Expected: `{"decision": "approve"}` (working tree is clean)

- [ ] **Step 5: Test script locally — config commit, dirty tree**

```bash
echo "test" > "$TEST_DIR/dirty-file.txt" && echo '{}' | bash plugins/smart/hooks/auto-commit.sh
```

Expected: `{"decision": "block", ...}` with systemMessage mentioning `/smart:commit`

- [ ] **Step 6: Clean up test artifacts**

```bash
rm -rf "$TEST_DIR" && unset TEST_DIR CLAUDE_PROJECT_DIR
```

Verify `git status` is clean — no test artifacts should remain in the repo.

- [ ] **Step 7: Commit**

```bash
git add plugins/smart/hooks/auto-commit.sh
git commit -m "feat: add auto-commit hook script with git status detection"
```

---

## Chunk 2: Config Skill (5 languages)

### Task 3: Create /smart:config skill — English (SKILL.md)

**Files:**
- Create: `plugins/smart/skills/config/SKILL.md`

Note: `user-invocable` frontmatter is intentionally omitted (defaults to `true`), because `/smart:config` must be user-accessible. This is unlike `check/SKILL.md` which uses `user-invocable: false`.

- [ ] **Step 1: Create SKILL.md**

```markdown
---
description: Configure auto commit/push behavior when tasks complete. Use when user says "config", "configure", "settings", "set auto commit", "set auto push", "turn on/off auto commit".
argument-hint: No arguments needed. Interactive configuration.
---

You are a plugin configuration assistant. Goal: let the user configure the auto-action behavior for the smart plugin.

Execution steps (must follow strictly in order):

1) Read current configuration:
- Check if `.claude/smart.local.md` exists in the project root.
- If it exists, read the `auto_action` value from its YAML frontmatter.
- If it does not exist or has no `auto_action` field, the current value is `off`.

2) Present options to the user using the AskUserQuestion tool:
- Show the current setting.
- Present the following choices:
  1. **commit** — auto commit when task completes (invokes /smart:commit)
  2. **push** — auto commit + push when task completes (invokes /smart:push)
  3. **off** — disable auto action (default)

3) Write the configuration:
- If user selects a new value, write/update `.claude/smart.local.md` with the YAML frontmatter:

```yaml
---
auto_action: <selected value>
---
```

- If the file already exists, update only the `auto_action` field, preserving any other content.
- If the file does not exist, create it with the frontmatter above.

4) Confirm to the user:
- Display the new setting.
- Remind: "Note: this change takes effect in the next Claude Code session. Please restart Claude Code for it to take effect."

Constraints:
- Do not modify any other files.
- Do not execute git commands.
- Valid values for `auto_action` are: `off`, `commit`, `push`.
```

- [ ] **Step 2: Commit**

```bash
git add plugins/smart/skills/config/SKILL.md
git commit -m "feat: add /smart:config skill (English)"
```

### Task 4: Create /smart:config skill — CN/TW/JA/KO translations

**Files:**
- Create: `plugins/smart/skills/config/SKILL_CN.md`
- Create: `plugins/smart/skills/config/SKILL_TW.md`
- Create: `plugins/smart/skills/config/SKILL_JA.md`
- Create: `plugins/smart/skills/config/SKILL_KO.md`

For each translation: translate the **complete** SKILL.md body — all execution steps (1-4), all constraints, and all AskUserQuestion details. Follow the same pattern as existing translation files (e.g., `commit/SKILL_CN.md` vs `commit/SKILL.md`). Technical terms like `auto_action`, `smart.local.md`, `AskUserQuestion` stay in English.

- [ ] **Step 1: Create SKILL_CN.md**

Frontmatter:
- `description`: 配置任务完成后自动 commit/push 的行为。当用户说"配置"、"设置"、"开启自动提交"、"关闭自动提交"时使用。
- `argument-hint`: 无需参数。交互式配置。

Body key translations:
- "You are a plugin configuration assistant" → "你是插件配置助手"
- "Read current configuration" → "读取当前配置"
- "Present options to the user" → "向用户展示选项"
- "Write the configuration" → "写入配置"
- "Confirm to the user" → "向用户确认"
- Reminder: "注意：此更改将在下一次 Claude Code 会话生效。请重新启动 Claude Code 以使其生效。"

- [ ] **Step 2: Create SKILL_TW.md**

Frontmatter:
- `description`: 配置任務完成後自動 commit/push 的行為。當用戶說「配置」、「設定」、「開啟自動提交」、「關閉自動提交」時使用。
- `argument-hint`: 無需參數。互動式配置。

Body: Full Traditional Chinese translation of all execution steps and constraints.
- Reminder: "注意：此變更將在下一次 Claude Code 會話生效。請重新啟動 Claude Code 以使其生效。"

- [ ] **Step 3: Create SKILL_JA.md**

Frontmatter:
- `description`: タスク完了時の自動 commit/push 動作を設定します。「設定」「自動コミットをオン」「自動プッシュをオフ」などの際に使用。
- `argument-hint`: 引数不要。対話式設定。

Body: Full Japanese translation of all execution steps and constraints.
- Reminder: "注意：この変更は次の Claude Code セッションで有効になります。Claude Code を再起動してください。"

- [ ] **Step 4: Create SKILL_KO.md**

Frontmatter:
- `description`: 작업 완료 시 자동 commit/push 동작을 구성합니다. "설정", "자동 커밋 켜기", "자동 푸시 끄기" 등의 경우 사용.
- `argument-hint`: 인수 불필요. 대화형 구성.

Body: Full Korean translation of all execution steps and constraints.
- Reminder: "참고: 이 변경 사항은 다음 Claude Code 세션에서 적용됩니다. Claude Code를 다시 시작하세요."

- [ ] **Step 5: Commit all translations**

```bash
git add plugins/smart/skills/config/SKILL_CN.md plugins/smart/skills/config/SKILL_TW.md plugins/smart/skills/config/SKILL_JA.md plugins/smart/skills/config/SKILL_KO.md
git commit -m "feat: add /smart:config skill translations (CN/TW/JA/KO)"
```

---

## Chunk 3: Version Bump & README Updates

### Task 5: Bump plugin version

**Files:**
- Modify: `plugins/smart/.claude-plugin/plugin.json`

- [ ] **Step 1: Update version 1.4.0 → 1.5.0**

In `plugins/smart/.claude-plugin/plugin.json`, change `"version": "1.4.0"` to `"version": "1.5.0"`.

- [ ] **Step 2: Commit**

```bash
git add plugins/smart/.claude-plugin/plugin.json
git commit -m "chore: bump plugin version to 1.5.0"
```

### Task 6: Update all README files

**Files:**
- Modify: `README.md` (English)
- Modify: `README_TW.md` (Traditional Chinese)
- Modify: `README_JA.md` (Japanese)
- Modify: `README_KO.md` (Korean)

Note: `README_CN.md` already has the Auto Hook section (added during design phase), but still needs `/smart:config` added to its slash commands table.

- [ ] **Step 1: Update README.md (English)**

Add an "Auto Hook" section before `## Requirements`, matching the structure in README_CN.md but in English:
- Section title: `## Auto Hook`
- Description: Automatically triggers commit or push when CC finishes a task via Stop Hook.
- Configuration: Run `/smart:config` to toggle (default: off).
- Include the flow diagram (translated to English).

- [ ] **Step 2: Update README_TW.md (Traditional Chinese)**

Add the same Auto Hook section in Traditional Chinese before `## 前置需求`.

- [ ] **Step 3: Update README_JA.md (Japanese)**

Add the same Auto Hook section in Japanese before "## 前提条件".

- [ ] **Step 4: Update README_KO.md (Korean)**

Add the same Auto Hook section in Korean before "## 사전 요구 사항".

- [ ] **Step 5: Also add `/smart:config` to the slash commands table in all 5 READMEs**

In each README's slash commands table, add a row for `/smart:config`:
- EN: `| /smart:config | Configure auto commit/push behavior |`
- CN: `| /smart:config | 配置自动提交/推送行为 |`
- TW: `| /smart:config | 配置自動提交/推送行為 |`
- JA: `| /smart:config | 自動コミット/プッシュの動作を設定 |`
- KO: `| /smart:config | 자동 커밋/푸시 동작 구성 |`

- [ ] **Step 6: Update .claude/CLAUDE.md project instructions**

Update the project structure section in `.claude/CLAUDE.md` to include the new `hooks/` directory and `config/` skill:

```
plugins/smart/
├── .claude-plugin/plugin.json
├── hooks/
│   ├── hooks.json        # Stop hook 配置
│   └── auto-commit.sh   # 自动提交/推送判断脚本
└── skills/
    ├── check/
    ├── commit/
    ├── config/   # 交互式配置（auto commit/push）
    ├── push/
    └── pr/
```

- [ ] **Step 7: Commit**

```bash
git add README.md README_CN.md README_TW.md README_JA.md README_KO.md .claude/CLAUDE.md
git commit -m "docs: add Auto Hook section and /smart:config to all READMEs"
```
